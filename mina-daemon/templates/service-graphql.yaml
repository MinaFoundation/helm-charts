{{- if .Values.node.exposeGraphql }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.deployment.name }}-{{ .Values.node.name }}-graphql
spec:
  type: ClusterIP
  publishNotReadyAddresses: true
  selector:
    app: {{ .Values.deployment.name }}-{{ .Values.node.name }}
  ports:
  - name: http-graphql
    protocol: TCP
    port: 80
    targetPort: {{ .Values.node.ports.graphql }}
{{- end }}
