{{- if .Values.autoscaling.bot.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "gpt-survey-summarizer.fullname" . }}
  labels:
    {{- include "gpt-survey-summarizer.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "gpt-survey-summarizer.fullname" . }}
  minReplicas: {{ .Values.autoscaling.bot.minReplicas }}
  maxReplicas: {{ .Values.autoscaling.bot.maxReplicas }}
  metrics:
    {{- if .Values.autoscaling.bot.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.autoscaling.bot.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.autoscaling.bot.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.autoscaling.bot.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
