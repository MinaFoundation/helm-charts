apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "block-producer-uptime.fullname" . }}-test-connection"
  labels:
    {{- include "block-producer-uptime.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "block-producer-uptime.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
