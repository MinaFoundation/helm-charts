apiVersion: v1
kind: Service
metadata:
  name: {{ include "mina-payout-reports.fullname" . }}
  labels:
    {{- include "mina-payout-reports.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mina-payout-reports.selectorLabels" . | nindent 4 }}
